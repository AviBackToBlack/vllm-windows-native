# Validation: vLLM v0.27.1 native Windows on RTX 5090 / SM120

Status: **Accepted candidate**

This document records the first fully accepted native-Windows single-GPU milestone for `vllm-windows-native`.

## Reproducible source delta

Authoritative upstream:

- repository: `https://github.com/vllm-project/vllm.git`
- tag: `v0.27.1`
- commit: `6e448d0ea9bf3d88d898b65449ca6dc2aec170ac`

Accepted implementation checkpoint:

- commit: `9f18048a2052072166ccc3b0ffef364e72bb4d11`
- tree: `115fa3e10d4b5a45c3e647cb343680a1fc743d18`

Project patchset:

- `patches/windows/v0.27.1/0001-native-windows-cuda-sm120.patch`
- SHA-256: `08416EBB43632A0AC485839644A98E875A4AD9EBC49BE193DA25AB804AE6A272`

The patch was generated directly by Git from the accepted implementation commit. It was then tested against a fresh detached `v0.27.1` worktree with `git apply --check` and `git apply --index`. The resulting Git tree object was:

`115fa3e10d4b5a45c3e647cb343680a1fc743d18`

which exactly matches the accepted implementation tree. This proves the patchset reproduces the accepted source bytes.

`*.patch` is forced to LF in `.gitattributes`; CRLF conversion can make mail-format patches fail to apply on Windows.

## Validated toolchain

- Windows 11 x64
- Python 3.13.15
- MSVC 19.44.35228 x64
- CUDA Toolkit 13.0.88
- CMake 4.4.3
- Ninja 1.13.2.git.kitware.jobserver-pipe-1
- PyTorch 2.13.0+cu130
- torchvision 0.28.0+cu130
- torchaudio 2.11.0+cu130
- Triton-Windows 3.7.1.post27
- target architecture: `TORCH_CUDA_ARCH_LIST=12.0`
- FlashAttention build selection: FA2

Build command:

```powershell
python -m build --wheel --no-isolation -C 'cmake.build-type=RelWithDebInfo'
```

## Accepted artifact

Wheel:

`vllm-0.27.2.dev0+g6e448d0ea.d20260909-cp313-cp313-win_amd64.whl`

- size: 176,829,605 bytes
- SHA-256: `EA763A7BBC7F3EE445C999F71597C82DF1E22A00715311EFCC2C6B46FB50D23A`
- ZIP integrity: PASS
- native `.pyd` count: 9

Native extensions:

1. `vllm/_C_stable_libtorch.pyd`
2. `vllm/_flashkda_C.cp313-win_amd64.pyd`
3. `vllm/_moe_C_stable_libtorch.pyd`
4. `vllm/_qutlass_C.pyd`
5. `vllm/cumem_allocator.pyd`
6. `vllm/fs_io_C.pyd`
7. `vllm/spinloop.pyd`
8. `vllm/third_party/deep_gemm/_C.cp313-win_amd64.pyd`
9. `vllm/vllm_flash_attn/_vllm_fa2_C.pyd`

## Pristine installation acceptance

The wheel was installed into a brand-new Python venv using normal dependency resolution:

```powershell
pip install --extra-index-url https://download.pytorch.org/whl/cu130 <wheel>
```

No `--no-deps`, source-tree overlay, manual Torch replacement, or copied runtime files were used.

Results:

- install exit 0
- `pip check`: PASS
- Torch: `2.13.0+cu130`
- CUDA runtime reported by Torch: `13.0`
- GPU: `NVIDIA GeForce RTX 5090`
- compute capability: `(12, 0)`
- native imports: 9/9 PASS
- `vllm --version`: PASS
- `vllm --help`: PASS
- `vllm serve --help`: PASS

## Offline default-performance acceptance

Model: `Qwen/Qwen3.5-0.8B`

The test used normal V1/default performance mode (`enforce_eager=False`) and exercised CUDA 13, FlashAttention 2, Triton-Windows, Inductor/AOT and CUDA Graph capture.

Results:

- engine ready: 111.99 s
- generation: 0.48 s
- prompt: `The capital of Ireland is`
- output begins with `Dublin.`
- marker: `VLLM_FINAL27_OFFLINE_OK`
- process shutdown: clean exit 0

## OpenAI-compatible HTTP acceptance

The pristine installation successfully started:

```powershell
vllm serve Qwen/Qwen3.5-0.8B --host 127.0.0.1 --port 8127 --max-model-len 2048 --gpu-memory-utilization 0.6 --disable-log-stats
```

Results:

- `GET /v1/models`: HTTP 200
- `POST /v1/completions`: HTTP 200
- prompt: `The capital of Ireland is`
- generated completion contains `Dublin`
- measured completion request: about 500 ms for 16 generated tokens
- scoped server process-tree cleanup left no vLLM/EngineCore processes and closed the test port

This validates the end-to-end path:

`HTTP -> FastAPI/Uvicorn -> AsyncLLM -> Windows ZMQ/TCP IPC -> EngineCore -> CUDA/FA2/Triton/Inductor/CUDAGraph -> generated tokens -> OpenAI response`

## Important limitations

- FA3 is intentionally absent because the upstream FA3 path does not support Blackwell SM120.
- DeepGEMM 2.6.1 Windows binary/JIT plumbing is built and importable, but upstream DeepGEMM has no SM120 runtime dispatch. Real `bf16_gemm_nt(...)` therefore reports `Unsupported architecture`; SM120 must not be routed into the SM100 backend blindly.
- This milestone accepts only one GPU. Windows NCCL, Tensor Parallel and Pipeline Parallel remain future work.
- A non-fatal PyTorch/c10d hostname connection warning (`WinError 10049`) remains visible during world-size-1 initialization and should be investigated before release polish.

## Provenance

`aivrar/vllm-windows-build` and `SystemPanic/vllm-windows` remain research/reference sources, not runtime dependencies. See ADR 0001. A line-level provenance/licensing review of any implementation material influenced by reference ports is still required before a public release.

Machine-readable details, including external source pins and dependency-patch hashes, are in `manifests/runtime/v0.27.1-rtx5090-sm120.json`.
